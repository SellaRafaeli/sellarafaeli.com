<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sella Rafaeli – Career Story</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #0e0e11;
      --fg: #f5f5f7;
      --muted: #a0a0a8;
      --accent: #7c7cff;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI",
        Roboto, Helvetica, Arial, sans-serif;
      background: radial-gradient(1200px 600px at 20% -10%, #1a1a22, var(--bg));
      color: var(--fg);
    }

    .slide {
      min-height: 100vh;
      padding: 80px 90px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 48px;
    }

    h1 {
      font-size: 64px;
      line-height: 1.05;
      margin: 0;
      letter-spacing: -0.02em;
    }

    h1 span {
      color: var(--accent);
    }

    .subtitle {
      font-size: 28px;
      color: var(--muted);
      max-width: 900px;
    }

    .points {
      display: grid;
      grid-template-columns: 1fr;
      gap: 28px;
      max-width: 1100px;
    }

    .point {
      font-size: 28px;
      line-height: 1.35;
    }

    .point strong {
      color: var(--accent);
      font-weight: 600;
    }

    .aside {
      display: flex;
      align-items: center;
      gap: 18px;
      font-size: 22px;
      color: var(--muted);
    }

    .dino {
      width: 56px;
      height: 56px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #1f1f2a;
      border-radius: 14px;
      font-size: 32px;
    }

    .quote {
      margin-top: 16px;
      padding-left: 20px;
      border-left: 4px solid var(--accent);
      font-style: italic;
      color: #d6d6ff;
      font-size: 26px;
    }

    .footer {
      margin-top: auto;
      font-size: 20px;
      color: var(--muted);
    }
  </style>
</head>
<body>
  <section class="slide">
    <header>
      <h1>
        I’m <span>Sella Rafaeli</span><br />
        Engineer, Builder, Founder
      </h1>
      <p class="subtitle">
        ex-8200 · early @ Fiverr · serial founding engineer · 2 exits (Hysolate, Wib)
      </p>
    </header>

    <div class="points">
      <div class="point">
        I finished the army <strong>before the Cloud</strong>, before smartphones —
      </div>

      <div class="aside">
        <div class="dino">🦖</div>
        <div>when dinosaurs still roamed the earth.</div>
      </div>

      <div class="point">
        Working at a startup is <strong>very</strong> good for your career.<br />
        Being a <strong>founding engineer</strong> can be career-defining —
        not just in title, but in real personal growth.
      </div>

      <div class="point">
        It’s the fastest way for an engineer to <strong>level up</strong> —
        technically <em>and</em> commercially.
        <br />
        (The next step up is going independent.)
      </div>

      <div class="quote">
        “If you're not the lead dog, the view never changes.”
      </div>

      <div class="point">
        Today, I run <strong>IndyDevs</strong> —
        a private network for senior tech talent and independent consultants for fun and profit.
      </div>
    </div>

    <div class="footer">
      Building careers · Building leverage · Building freedom · indydevs.com · 052-293-4321
    </div>
  </section>
</body>
</html>
